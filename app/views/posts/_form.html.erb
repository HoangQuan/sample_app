<%= form_for @post do |f| %>
<%= render "application/errors", object: f.object %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :content %>
	<%= f.cktext_area :content,  class: %W(ckeditor) %>
	<%= f.submit "Send", class: "btn btn-large btn-primary" %>
<% end %>