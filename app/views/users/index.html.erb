<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
   <% @users.each do |user| %>
  <!-- <table align="center">
		<tr>
			<td><%= gravatar_for user, size: 52 %></td>
			<td><%= link_to user.name, user %></td>
		</tr>
  </table> -->

  <!-- binh thuong thi lam the nay -->
    <!-- <li>
      <%= gravatar_for user, size: 52 %>
      <%= link_to user.name, user %>
    </li> -->
    <!-- <% end %> -->
    <%= render @users %>
  
</ul>

<%= will_paginate %>